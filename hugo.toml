baseURL = 'https://briannabeehler.com/'
languageCode = 'en-us'
title = 'Brianna Beehler'
theme = 'ananke'

[params]
  # Typography & background
body_classes = "garamond bg-near-white"
  # text_color = ""

  # Hero/header styling
  # background_color_class = "bg-blue"
  # cover_dimming_class = "bg-black-60"
  # featured_image = "/images/hero.jpg"

  # Branding
  # site_logo = "/images/logo.png"

# Content
show_reading_time = false

[params.ananke]
show_recent_posts = false
  # read_more_copy = "Read more"
  # date_format = "January 2, 2006"

  # Custom CSS (place in assets/ananke/css/)
  # custom_css = ["custom.css"]

  # Font options: avenir, athelas, georgia, times, bodoni, calisto, garamond, baskerville, helvetica, courier
  # Color options: bg-blue, bg-dark-blue, bg-navy, bg-green, bg-dark-green, bg-gold, bg-orange, bg-red, bg-pink, bg-purple, bg-black, bg-near-black, bg-dark-gray, bg-gray, bg-near-white, bg-white

